const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function api(url,opt){const r=await fetch(url,opt);const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||`HTTP ${r.status}`);return j}
async function copyText(text){try{await navigator.clipboard.writeText(text);return true}catch{return false}}
function videoUrl(kind,id){return kind==='clean'?`/storage/clean/${encodeURIComponent(id)}-clean.mp4`:`/storage/social/${encodeURIComponent(id)}.mp4`}
function mediaBox(title,url,note){return `<div class="video-box"><h4>${esc(title)}</h4><video controls preload="metadata" src="${esc(url)}"></video><div class="muted">${esc(note)}</div></div>`}
async function loadStats(){const s=await api('/api/discovery/dashboard');$('#stats').innerHTML=`<span class="status-pill">대상 ${s.targets}</span><span class="status-pill">게시물 ${s.posts}</span><span class="status-pill">Instagram ${s.instagram||0}</span><span class="status-pill ready">자동확정 ${s.autoConfirmed}</span><span class="status-pill">검토 ${s.reviewRequired}</span><span class="status-pill">콘텐츠 ${s.contentJobs}</span>`}
async function loadTargets(){const j=await api('/api/discovery/targets');$('#targets').innerHTML=(j.items||[]).map(x=>`<div class="card"><b>${esc(x.platform)} · ${esc(x.target_type)}</b><div>${x.platform==='INSTAGRAM'?'@':''}${esc(x.target_value)}</div>${x.inpock_url?`<div class="muted">인포크 ${esc(x.inpock_url)}</div>`:''}<div class="row"><span class="status-pill ${x.enabled?'ready':''}">${x.enabled?'활성':'중지'}</span><button class="btn-secondary toggle-target" data-id="${esc(x.id)}" data-next="${x.enabled?0:1}">${x.enabled?'중지':'활성'}</button></div></div>`).join('')||'<p class="empty">대상이 없습니다</p>';document.querySelectorAll('.toggle-target').forEach(b=>b.onclick=async()=>{await api('/api/discovery/targets/'+encodeURIComponent(b.dataset.id),{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({enabled:Number(b.dataset.next)===1})});await loadTargets();await loadStats()})}
function renderCandidate(c){if(!c)return '<div class="ai-box"><b>AI 상품 판독</b><div class="muted">아직 분석 전입니다</div></div>';let features=[];try{features=JSON.parse(c.features_json||'[]')}catch{}return `<div class="ai-box"><b>AI 상품 판독</b><h3>${esc(c.korean_name||'상품명 미확정')}</h3><div>${esc(c.product_type||'')}</div>${features.length?`<div class="muted">${features.map(esc).join(' · ')}</div>`:''}<div class="muted">판독 신뢰도 ${Math.round(Number(c.resolver_confidence||0))}%</div></div>`}
function renderInstagram(meta){const ig=meta?.instagram;if(!ig)return '';const r=ig.rewrittenScript||{};return `<div class="script-box"><b>Instagram 원본 대본 → 새 대본</b><div class="muted" style="margin-top:8px">원본 음성 전사</div><div>${esc(ig.transcript||'음성 없음')}</div><div class="muted" style="margin-top:10px">재작성 대본</div><div><b>${esc(r.fullScript||[r.hook,r.body,r.ending].filter(Boolean).join(' ')||'')}</b></div>${ig.inpockUrl?`<div class="muted" style="margin-top:8px">인포크 ${esc(ig.inpockUrl)}</div>`:''}${ig.inpockMatch?`<div class="muted">인포크 매칭 ${esc(ig.inpockMatch.title||'상품')} · ${Math.round(Number(ig.inpockMatch.score||0))}점</div>`:''}</div>`}
function renderMatch(m){const confirmed=m.status==='AUTO_CONFIRMED';return `<div class="match ${confirmed?'confirmed':''}">${m.product_image?`<img src="${esc(m.product_image)}" alt="상품 이미지">`:'<div></div>'}<div><b>${esc(m.product_name||'')}</b><div>${Number(m.product_price||0).toLocaleString()}원</div><div class="score">매칭 ${Math.round(Number(m.score||0))}점 · ${esc(m.status)}</div><div class="row">${!confirmed?`<button class="btn-secondary confirm" data-id="${esc(m.id)}">이 상품 확정</button>`:''}${m.product_url?`<a class="btn-secondary" href="${esc(m.product_url)}" target="_blank" rel="noopener">쿠팡 상품 보기</a>`:''}</div>${confirmed?`<div class="affiliate"><b>내 쿠파스 링크</b><br>${esc(m.affiliate_url||'딥링크 생성 대기')}</div><div class="row">${m.affiliate_url?`<button class="btn-primary copy-link" data-link="${esc(m.affiliate_url)}">링크 복사</button>`:''}<button class="btn-secondary create-short" data-id="${esc(m.id)}">새 대본으로 쇼츠 제작</button></div>`:''}</div></div>`}
async function loadPosts(){const j=await api('/api/discovery/posts');const box=$('#posts');box.innerHTML='';for(const p of j.items||[]){const d=await api('/api/discovery/posts/'+encodeURIComponent(p.id));const matches=d.matches||[];const original=videoUrl('original',p.id),clean=videoUrl('clean',p.id);const originLabel=p.platform==='INSTAGRAM'?'Instagram 원본 Reel':'중국 원본 게시물';box.insertAdjacentHTML('beforeend',`<div class="card"><div class="toolbar"><div><div class="row"><span class="status-pill">${esc(p.platform)}</span><span class="status-pill ${p.status==='AUTO_CONFIRMED'?'ready':''}">${esc(p.status)}</span></div><h3 class="post-title">${esc(p.caption||p.external_post_id)}</h3><a href="${esc(p.source_url)}" target="_blank" rel="noopener">${originLabel} 열기</a></div><button class="btn-primary analyze" data-id="${esc(p.id)}">${p.status==='DISCOVERED'?'분석 시작':'다시 분석'}</button></div><div class="review-grid"><div><h3 class="section-title">영상 확인</h3><div class="video-grid">${mediaBox('원본 영상',original,'다운로드된 원본')}${mediaBox('일반 자막/설명 텍스트 클리닝본',clean,'상품 매칭 후 생성')}</div>${renderInstagram(d.post?.metadata||{})}</div><div><h3 class="section-title">쿠팡 상품 매칭</h3>${renderCandidate(d.candidate)}<div>${matches.length?matches.map(renderMatch).join(''):'<div class="empty-video">분석 후 쿠팡 후보가 여기에 표시됩니다</div>'}</div></div></div></div>`)}document.querySelectorAll('.analyze').forEach(b=>b.onclick=async()=>{b.disabled=true;b.textContent='분석 중…';try{await api('/api/discovery/posts/'+encodeURIComponent(b.dataset.id)+'/analyze',{method:'POST',headers:{'content-type':'application/json'},body:'{}'});await loadPosts();await loadStats()}catch(e){alert(e.message)}finally{b.disabled=false}});document.querySelectorAll('.confirm').forEach(b=>b.onclick=async()=>{try{const j=await api('/api/discovery/matches/'+encodeURIComponent(b.dataset.id)+'/confirm',{method:'POST'});await loadPosts();await loadStats();if(j.affiliateUrl)alert('상품 확정 완료\n내 쿠파스 링크도 생성됐습니다')}catch(e){alert(e.message)}});document.querySelectorAll('.copy-link').forEach(b=>b.onclick=async()=>alert(await copyText(b.dataset.link)?'쿠파스 링크를 복사했습니다':'복사에 실패했습니다'));document.querySelectorAll('.create-short').forEach(b=>b.onclick=async()=>{try{const j=await api('/api/discovery/matches/'+encodeURIComponent(b.dataset.id)+'/create-short',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({contentScore:85})});localStorage.setItem('globalShortsProjects',JSON.stringify([j.projectId,...JSON.parse(localStorage.getItem('globalShortsProjects')||'[]').filter(x=>x!==j.projectId)].slice(0,30)));alert('쇼츠 제작을 시작했습니다')}catch(e){alert(e.message)}})}
$('#platform').addEventListener('change',()=>{const ig=$('#platform').value==='INSTAGRAM';if(ig)$('#targetType').value='ACCOUNT';$('#targetType').disabled=ig;$('#inpockUrl').closest('label').style.display=ig?'block':'none'});$('#platform').dispatchEvent(new Event('change'));
$('#addTarget').onclick=async()=>{try{await api('/api/discovery/targets',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({platform:$('#platform').value,targetType:$('#targetType').value,targetValue:$('#targetValue').value.trim(),inpockUrl:$('#inpockUrl').value.trim()})});$('#targetValue').value='';$('#inpockUrl').value='';await loadTargets();await loadStats()}catch(e){alert(e.message)}};
$('#ingest').onclick=async()=>{try{await api('/api/discovery/ingest',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({platform:$('#postPlatform').value,externalPostId:$('#externalPostId').value.trim(),sourceUrl:$('#sourceUrl').value.trim(),videoUrl:$('#videoUrl').value.trim(),caption:$('#caption').value.trim()})});['externalPostId','sourceUrl','videoUrl','caption'].forEach(id=>$('#'+id).value='');await loadPosts();await loadStats()}catch(e){alert(e.message)}};$('#refresh').onclick=()=>Promise.all([loadPosts(),loadStats()]);Promise.all([loadStats(),loadTargets(),loadPosts()]).catch(e=>alert(e.message));